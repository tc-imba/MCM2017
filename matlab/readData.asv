function A=readData(route)
    order_list=['asc ';'desc'];
    type_list=['norm','busy'];
    auto_list=['0.0';'0.1';'0.3';'0.5';'0.7';'0.9';'1.0'];
    for i=1:length(order_list)
        for j=1:length(type_list)
            for k=1:length(auto_list)
                str=['../output/',route(),'_',order_list(i,:),'_',type_list(j,:),'_',auto_list(k,:),'.txt']
            end
        end
    end
end